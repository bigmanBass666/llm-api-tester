# Checklist

## 阶段一：模型详情页探索
- [x] DeepSeek 模型详情页探索完成 ✅
  - 验证方法：成功访问 deepseek-v3_2 详情页并提取 API 代码
  - 发现：API ID 应为 `deepseek-v3.2`（点号），不是 `deepseek-v3_2`（下划线）
  
- [x] Meta Llama 模型详情页探索完成 ✅
  - 验证方法：成功访问 llama-3_1-8b-instruct 详情页并提取 API 代码
  - 发现：API ID 应为 `llama-3.1-8b-instruct`（点号），不是 `llama-3_1-8b-instruct`（下划线）

- [x] 成功模型对照探索完成 ✅
  - 验证方法：成功访问 mistral-nemotron 详情页并提取 API 代码
  - 发现：URL 和 API ID 完全一致，无格式差异

## 阶段二：分析总结
- [x] ID 差异对比完成 ✅
  - 验证结果：**失败模型**的 URL 用下划线 `_`，但 **API 需要点号 `.`
  - 规律：NVIDIA 网页 URL 使用下划线，但实际 API 调用需要转换为点号
  
- [x] 根因分析完成 ✅
  - 根因：**ID 格式不一致导致 404 错误**
  - 影响：预计可修复 **20+ 个模型**
  
- [x] 探索报告输出 ✅
  - 结论明确：需要在爬虫中添加 ID 格式转换逻辑
  - 修复方案：`model_id.replace('_', '.')`

## 整体验收标准
- [x] 成功提取 NVIDIA 网页的 API 调用代码示例 ✅
- [x] 明确 404 错误的根本原因（ID 格式不一致）✅
- [x] 提出可行的修复方案（下划线转点号）✅
- [x] 探索报告清晰完整 ✅
