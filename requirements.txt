# API 测试项目依赖

# 核心依赖
openai>=1.0.0
httpx>=0.27.0
python-dotenv>=1.0.0  # 环境变量加载

# 爬虫依赖（可选）
playwright>=1.40.0
beautifulsoup4>=4.12.0

# 测试依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 工具依赖
tqdm>=4.65.0
pyyaml>=6.0

# Windows SSL 证书（自动安装）
certifi>=2023.0.0
